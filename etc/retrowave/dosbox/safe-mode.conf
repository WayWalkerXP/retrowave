[sdl]
fullscreen=false

[dosbox]
memsize=16

[render]
aspect=false

[cpu]
core=normal
cycles=fixed 12000

[autoexec]
@echo off
mount c /var/lib/retrowave/drives/c
c:
if exist AUTOEXEC.BAT call AUTOEXEC.BAT
